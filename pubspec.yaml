name: asitente_vial
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Gestión de Estado
  provider: ^6.1.2

  # Networking (Cliente HTTP robusto ideal para APIs complejas)
  dio: ^5.7.0

  # Inteligencia Artificial (SDK Oficial de Google Gemini - Versión compatible con tu entorno)
  google_generative_ai: ^0.4.7

  # Base de Datos Local Offline
  sqflite: ^2.4.1
  path: ^1.9.0
  sqflite_common_ffi: ^2.4.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true